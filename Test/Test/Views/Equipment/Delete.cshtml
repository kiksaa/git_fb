@model Test.Models.equipment

@{
    Layout = "~/Views/Shared/_Layaccount.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>FARMBOOK - ลบทะเบียนอุปกรณ์</title>
</head>
<body>
    <br />
    <div class="container">
        <div class="row justify-content-md-center">
            <h3>คุณแน่ใจที่จะลบข้อมูลใช่หรือไม่</h3>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="container">
                    <div class="form-actions no-color">
                        <a type="button" class="btn btn-danger" href="@Url.Action("Index", "Equipment")">
                            <span>
                                ยกเลิก
                            </span>
                        </a>
                        <input type="submit" value="ลบ" class="btn btn-success me-md-2" />
                    </div>
                </div>
            }
        </div>
    </div>
    <div>
        <hr />
        <div class="container">
            <div class="row justify-content-md-center">
                <div class="card" style="width: 50rem;">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            @Html.DisplayNameFor(model => model.vehicleType) :
                            @Html.DropDownListFor(model => model.vehicleType, (IEnumerable<SelectListItem>)ViewBag.vehicles, "เลือกประเภทยานพาหนะ", htmlAttributes: new { @class = "form-control" })
                        </li>
                        <li class="list-group-item">
                            @Html.DisplayNameFor(model => model.vehicleID) :
                            @Html.DisplayFor(model => model.vehicleID)
                        </li>
                        <li class="list-group-item">
                            @Html.DisplayNameFor(model => model.vehicleName) :
                            @Html.DisplayFor(model => model.vehicleName)
                        </li>
                        <li class="list-group-item">
                            @Html.DisplayNameFor(model => model.dateBuy) :
                            @Html.DisplayFor(model => model.dateBuy)
                        </li>
                        <li class="list-group-item">
                            @Html.DisplayNameFor(model => model.vehicleImg) :
                            @Html.DisplayFor(model => model.vehicleImg)
                            <img src="~/Content/img/upload/vehicle/@Model.vehicleImg" height="250" width="240">
                        </li>

                    </ul>
                </div>
            </div>
        </div>

    </div>
</body>
</html>
